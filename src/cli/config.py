from enum import Enum


class Commands(Enum):
    ADD = "add"
    FIND = "find"
    UPDATE = "update"

    BIRTHDAY = "birthday"
    ADD_BIRTHDAY = "add-birthday"

    ADDRESS = "address"
    ADD_ADDRESS = "add-address"

    EMAIL = "email"
    ADD_EMAIL = "add-email"
    UPDATE_EMAIL = "update-email"
